'use strict';

/**
 * menuimg.js — Menu image support
 * Images removed for Railway/cloud compatibility.
 * Returns null so the menu renders as text-only.
 */
async function getMenuImage() {
    return null;
}

module.exports = { getMenuImage };
